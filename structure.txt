Game system Terminal

File System

struct entry
-- name 
-- path
-- parent
-- is_folder()

file struct
-- entry
-- content (text)

folder struct
-- entry
-- vector of entries

functions folder
- make folder ( root, folder_name )
- display_folders_contents ( root )
- open_folder ( current, folder_name )
- remove_folder( current, folder_name )
- display_tree ( current, depth )
- rename_folder_name( current, folder_name, rename_name )